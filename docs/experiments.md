# scripts/ 索引：每个脚本问的什么、结论是什么、还算不算数

`scripts/` 下有 34 个脚本，编号能排序但看不出三件事：**哪个是生产每天在跑的、
哪个的结论已经被后续实验推翻、哪个会真的下单**。这份表补上。

> **最后更新：2026-09-08。** 加实验脚本时一并在这里加一行 —— 这份表的价值
> 完全取决于它是不是最新的。尤其是「已被推翻」那一列：这轮里有两个结论被
> 后续实验翻掉了，如果没人记，半年后看到那些脚本会当成有效结论。

---

## ⚠️ 先看这个：五组编号撞车

编号本来是给「按顺序跑」用的，现在有五对同号：

| 编号 | 两个脚本 |
|---|---|
| 08 | `08_regime_counterfactual.py` / `08_test_agents.py` |
| 20 | `20_daily_review.py` / `20_timing_gate.py` |
| 21 | `21_analyze.py` / `21_blend_gate.py` |
| 22 | `22_expand_universe.py` / `22_params.py` |
| 24 | `24_fill_slippage.py` / `24_llm_check.py` |

**不要靠编号指代脚本**，写全名。P8 那一组（20/21/22 的 review/analyze/params）
是 `plan.md` §4.2 早就规划好的编号，后加的实验脚本撞了上去。
新增实验脚本请从 **26** 往后编。

## 🔴 会真的下单的脚本

只有一个：`tools/validate_ths_p9.py`（三道闸：PAPER + 模拟账户 + 交易时段）。
`scripts/` 下**没有**任何脚本会直接下单 —— 下单只在 `daily_cycle` 里发生，
而它需要三把锁。详见 `CLAUDE.md` §七。

---

## 一、生产流水线（每天在跑，不是实验）

| 脚本 | 干什么 |
|---|---|
| `00_market_check.py` | 今天是不是交易日。**退出码不可信**，只认输出里的 `'trading_day': False`（`CLAUDE.md` §八） |
| `01_ingest.py` | 拉日线 → parquet 增量缓存 → 股票池 → qlib bin |
| `02_train.py` | Alpha158 + LightGBM 三 seed 集成 |
| `03_predict.py` | 读最近一次预测，产出候选 CSV |
| `04_plan_orders.py` | 模型分数 → 风控闸 → 下单清单 |
| `05_report.py` | 中文日报 |
| `14_backfill_store.py` | 从 JSONL 重建 `data/runs.db`（store 是派生的，删掉不影响下单） |
| `26_premarket.py` | **盘前**：拉数 + 重训 + 逐票复核 → `data/reviews/<date>.json`。**永远不下单**（没有风控闸、没有 execution）。日流程按「候选名单逐只相同」决定用不用它 |
| `27_notify.py` | 预览/补发日报邮件 |
| `email_listener.py` | 入站邮件命令（重跑 / 状态 / 关机），默认关闭 |
| `patch_tradingagents.py` | 往 vendored TradingAgents 注册 ashare vendor，幂等 |

**P8 复盘调参链**（`plan.md` §7-P8）：`20_daily_review.py`（每日复盘）、
`21_analyze.py`（零 LLM 事实层）、`22_params.py`（参数应用/回滚）。

## 二、工具（有用，但不产出「结论」）

| 脚本 | 干什么 |
|---|---|
| `06_backtest.py` | 回测入口。占位打分（momentum / reversal / random）+ model |
| `08_regime_counterfactual.py` | 某一段 risk-off 到底赚了还是亏了，事后算账 |
| `08_test_agents.py` | P7 单票在线复核 + 评级稳定性 |
| `11_rerender_reports.py` | 用当前渲染代码从 JSONL 重刷历史日报 |
| `22_expand_universe.py` | 把另一个指数的成分拉进 parquet，写**独立**的 universe 文件 |
| `24_fill_slippage.py` | 实测成交滑点。**数据待攒，至少 20 笔才有意义** |
| `24_llm_check.py` | 脱敏检查中转站配置，默认不联网 |

---

## 三、实验脚本：结论与有效性

**「还算不算数」这一列是这份表的全部意义。**

| 脚本 | 问的什么 | 结论 | 还算不算数 |
|---|---|---|---|
| `07_regime_stress.py` | SMA 该取多少（model-free 等权全池） | SMA=100 | ⚠️ **部分作废** —— 那张表跑在**带纳入前视**的指数上。见 `18_regime_recheck.py` |
| `09_regime_ab.py` | 择时该不该开（真实 k=3） | 该开，SMA=100 | ⚠️ **数字要打折** —— 原版有截断历史算 SMA 的 bug（2026-09-08 已修，但**表未重跑**） |
| `10_neutralize_gate.py` | `QBG_INDUSTRY_NEUTRAL` 该不该关 | **维持 =1** | ✅ 有效。告警是假的：优势全来自单个 5 个月窗口，子区间闸 1/4 |
| `12_rebalance_gate.py` | 调仓间隔该不该拉长 | **改成每 10 日**。修正复利 bug 后两个窗口都八项全过、子区间 4/4：388 日年化 37.39% → 58.64%、回撤 −47.09% → −35.41%；630 日 13.67% → 53.70%、回撤 −52.77% → −37.63% | ✅ 有效，**已部署**（2026-09-14）。❌ **原结论"维持每日"已推翻**：这道闸此前漏传相位、`keep_rank`、滑点，每一样都单向偏向日频。**复核要传 `--baseline 1`**，否则现行配置已是 10，会变成自己比自己 |
| `13_factor_zoo.py` | 27 个手写因子有没有用 | **换手率单独解释净年化差异的 58%**；没有一个因子在修掉偏差后站得住 | ✅ 有效，且是很多结论的地基 |
| `15_turnover_sweep.py` | 压换手能不能救 `bias20` | 能，但**只有迟滞那条路干净**：日频+迟滞30 → +34.43%、夏普 1.00、相位极差 **0%**（基准 +16.44%/0.88）；日频无迟滞只有 +14.73%/0.56，跑输基准 | ❌ **已被推翻** —— `17_survivorship.py` 修掉纳入前视后 bias20 间隔5 夏普 **0.56 < 同口径基准 0.64** ✅ 2026-09-20 在修正复利 bug 的引擎上重跑，推翻依然成立。**新发现：压换手的两种手段不等价** —— 拉长间隔要抽相位彩票，迟滞不用，见下方「怎么压换手」 |
| `16_horizon_sweep.py` | 模型的 alpha 有几天 | **h=1 最优**：118.86%/夏普 1.79 > h=10 92.48%/1.65 > h=5 76.81%/1.41 > h=3 62.78%/1.14 | ✅ 2026-09-20 重跑后有效。⚠ **这个脚本此前有两个独立缺陷**：复利 bug，外加它是四个实验脚本里**唯一漏掉相位平均**的一个。只跑相位 0 时它报 h=3 = +146.76%、夏普 2.19，**看着完胜 h=1** —— 而那正是 h=3 三个相位里最高的那一相（相位极差 143.47%）。两个都修掉后原结论不变 |
| `17_survivorship.py` | 纳入前视有多大 | **基准自己就从 +16.44% 掉到 +11.18%**（−5.26 点）；集中持仓受损 −10.8~−22.1 点 | ✅ 有效，2026-09-20 已在修正复利 bug 的引擎上重跑（原记录 +16.84%→+11.50%、−12~−23 点，同一量级）。**读任何绝对收益前先看这条** |
| `18_regime_recheck.py` | 择时抖动 / 缓冲带 / 指数口径 | 加缓冲带；**40 格无一夏普超过「一直满仓」的 0.90** | ✅ 有效 |
| `19_band_gate.py` | 缓冲带 / SMA 的八项闸 | `BAND=0.03` + `INDEX_ELIGIBLE=1` **捆绑**通过；`SMA 100→200` 通过 | ✅ 有效，**已部署** |
| `20_timing_gate.py` | 该不该关掉择时 | **不关** —— k=3 上回撤翻倍（−16.14% → −32.05%） | ✅ 有效。这个闸**注定过不了 `drawdown`**，那是设计使然 |
| `21_blend_gate.py` | 短线信号 × qlib 加权 | **三种形式都没跑赢纯模型**（连续/阈值/否决） | ✅ 有效。**同时推翻了「短线信号当否决器用」这条早先的建议** |
| `23_oos_window.py` | OOS 窗口该多长 | `mid` 最优，重跑后**理由更强**：相位极差每3日 123.6%→68.3%、每5日 104.6%→33.6%（噪声底砍 45~68%），而 **Rank IC 一点没掉**（cur +0.0207 → mid +0.0208）；`long` 撞悬崖（Rank IC +0.0070，−66%） | ✅ 有效，**未部署**。2026-09-20 已在修正复利 bug 的引擎上重跑。原记录是「Rank IC 只掉 18%」，现在是**零代价** —— 换 `mid` 的理由比当初更充分 |
| `25_universe_gate.py` | 扩池到中证500 值不值 | **不值。** `cur` 未过 4 项，`mid`（测试段长 65%）未过 **5 项**、子区间 1/4，20bp 滑点下净年化 **−19.17%**、夏普 −0.04 | ✅ 有效。2026-09-11 已按当初的备注在 `mid` 分段上重测（`--split mid`，**两条臂都重训**），结论不但没翻还更硬。子区间仍摆动 −268%，说明那不是窗口长度的问题 |
| `29_trailing_gate.py` | 退出规则：止损 / 移动止盈 / 现金触发 | **止损 8%：两个窗口都没过**（收益 +13.8 / −4.3 点，8% 是尖峰），但回撤两边都少 5.1 点、相位极差大降 —— **按操作者要求保留开启**。**移动止盈 10/10：没过**（收益两边都为正，但 630 日回撤变差 2.4 点、在尖峰上）。现金触发维持 0.5 | ✅ 有效。当天第一版移动止盈的数字来自有复利 bug 的引擎、且没模拟现金触发，已替换。**只跑一个窗口的结论不能用** —— 第一版 388 日上 16 组参数全部变好 |
| `28_shortterm_pools.py` | 打板 / 龙虎榜 / 短线因子能不能做 | **都不能。** 涨停次日超额 −0.73%（4 连板 −1.69%）；龙虎榜所有分档为负；短线因子截面 IC 0.032~0.036 是真的，但**集中到 top-3 时五个因子全部翻成负** | ✅ 有效，**本项目最稳的结论之一**：11 个年度 0 个翻车、三个板块全负、流动性上单调。详见 `docs/shortterm-pools.md` |

---

## 四、读这些结论前必须知道的三件事

按重要性排：

1. **生存者偏差污染的不只是策略，还有基准和择时信号。**
   等权持有 +16.84% → +11.50%，择时同参数夏普 0.75 → 0.63。
   **跨脚本引用任何绝对百分比之前，先确认两边同口径。**（`17_survivorship.py`）

### 怎么压换手：迟滞和拉长间隔不是一回事

2026-09-20 在修正复利 bug、且全部相位平均的引擎上重跑 `15_turnover_sweep.py`
才看出来的。两种手段都能把换手压下去，代价完全不同：

| 配置 | 换手 | 净年化 | 相位极差 | 超出基准 |
|---|---:|---:|---:|---:|
| k=3 间隔1 **迟滞30** | 0.116 | +34.43% | **0.00%** | +17.99 点 |
| k=3 间隔5 迟滞0 | 0.145 | +32.65% | 10.34% | +16.21 点 |
| k=3 间隔3 迟滞10 | 0.132 | +34.25% | **29.05%** | +17.81 点 |
| k=3 间隔10 迟滞10 | 0.078 | +29.79% | **33.35%** | +13.35 点 |

**日频+迟滞30 换手更低、年化更高，而且相位极差是 0** —— 日频每天都调仓，
根本没有"从哪天开始"这个自由度。拉长间隔的那几格，极差普遍**大于**它们
跑赢基准的幅度，按这个脚本自己的第 4 条判据就该淘汰。

这只在 `bias20` 因子上成立。**2026-09-20 当天就在生产模型上补测了那一格，
结论相反 —— 见下。**

### 那一格补测了：生产模型上「日频+高迟滞」全面落败

`12_rebalance_gate.py` 加了 `--candidate-keep-rank`（候选臂单独的迟滞）和
`--neighbors-keep`（高原闸改在迟滞维度上量），然后两个窗口都跑了。

**两个窗口各拿自己那一侧最优的迟滞去闸，都是 4 项未过**：

- cur（迟滞30）：未过 net_return、sharpe、turnover、subperiod(1/4)。
  +30.02%/夏普 0.79，而现行的每10日+迟滞15 是 **+58.71%/1.16**。
- mid（迟滞60，mid 上看着最好的那个）：未过 net_return、turnover、
  subperiod(1/4)、**plateau** —— 邻居迟滞50 的夏普只有 0.6955，比候选低 0.37，
  闸直接判成尖峰。

换手扫描说明了为什么这一格结构上就没戏（换手闸上限 = 基线 × 1.20）：

| 配置 | cur 换手 | mid 换手 | cur 净年化/夏普 | mid 净年化/夏普 |
|---|---:|---:|---:|---:|
| 每10日/迟滞15（现行） | **0.0734** | **0.0750** | +58.71% / 1.16 | +54.49% / 1.11 |
| 日频/迟滞15 | 0.5321 (7.3x) | 0.5732 (7.6x) | +39.89% / 0.92 | +14.61% / 0.52 |
| 日频/迟滞50 | 0.3455 (4.7x) | 0.3789 (5.0x) | +48.95% / 1.06 | +24.71% / 0.70 |
| 日频/迟滞60 | 0.3252 (4.4x) | 0.3645 (4.9x) | +36.22% / 0.88 | +51.19% / 1.06 |
| 日频/迟滞80 | 0.3000 (**4.1x**) | 0.3206 (**4.3x**) | +32.37% / 0.82 | +42.47% / 0.97 |

**迟滞开到 80（296 只里的前四分之一都算"还能留着"）仍然是基线换手的 4 倍多。**
而且两个窗口的"最优迟滞"互相不转移：cur 的赢家是 50，到 mid 上只有
+24.71%/0.70；mid 的赢家是 60，到 cur 上只有 +36.22%/0.88。**那不是高原，是噪声。**

**机制：压换手的手段要配信号的转换速度。** `bias20` 是 20 日均线偏离，
排名走得慢，迟滞能咬住持仓 —— 迟滞 0→30 把换手从 0.360 压到 0.116（3.1 倍）。
qlib 模型的 top-3 每天大幅换血，迟滞 15→80 只从 0.53 压到 0.30（1.8 倍），
**咬不住**。对这种信号，唯一真能少交易的办法就是少看它，也就是拉长间隔。

所以 `12_rebalance_gate.py` 的「每 10 日」**没有被推翻，反而多了一条证据**。

2. **模型测试段只有 388 天，相位噪声可达 142 个百分点年化。**
   小于约 100 个百分点的差异读不出统计意义。凡是标着「k=3 交叉臂」的都受这条
   约束。`23_oos_window.py` 的 `mid` 分段能把它降到约 49。

3. **Rank IC 在 k=3 上是差代理。** 反复出现：5 日反转 Rank IC 高于模型而收益只有
   1/20；扩池后 Rank IC 只掉 16% 而 top-3 塌了三分之二。它度量全截面排序，
   而 top-3 只用到最顶端三个位置。

## 五、已知还没做的事

- **`15` / `16` / `17` / `23` 跑在有复利 bug 的引擎上**（2026-09-14 修：多日持仓时漂移权重没有按组合收益归一化）。它们的结论方向多半还成立，但**数字没重跑**，引用前先重跑。
- `09_regime_ab.py` 的截断 SMA bug 已修，**但表没重跑**（`12_rebalance_gate.py` 已于
  2026-09-14 连同相位平均一起重跑，见上表）。
  重跑之前，`docs/p3-model-validation.md` 里引用它们的数字要按「可能偏乐观」读。
- 五个脚本（`00` / `05` / `14` / `21_analyze` / `22_params`）**没有 docstring**，
  用途只能从 `plan.md` §4.2 反查。
- `24_fill_slippage.py` 的数据要每天跑才攒得起来，目前**样本为零**。
